// LinkedList.h

#pragma once

namespace Utilities {
	
	template<class T>
	class LinkedList
	{
		public:
			LinkedList(){}

			void Add(T data)
			{

			}

			void Remove(int index)
			{

			}

			T * operator[](int)
			{
				int * a = new int (1);
				return a;
			}
	};
}
